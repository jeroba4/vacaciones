// Servicios de vacaciones

const Vacation = require('../models/vacationModel');

/**
 * Calcula el saldo de días atrasados.
 * @param {number} userId - ID del usuario.
 * @returns {number} - Días atrasados.
 */
exports.calculateOverdueDays = async (userId) => {
    // Lógica para calcular días atrasados
    return 0; // Placeholder
};
